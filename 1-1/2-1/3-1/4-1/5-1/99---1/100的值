#include <stdio.h>
int main ()
{
	int i;
	double sum = 0 ;
	printf("计算1-1/2+1/3-1/4+1/5 …… + 1/99 - 1/100的值：\n");
	for(i = 1;i <= 100;i++)
	{	
		if(i%2 != 0)
		{
			sum = sum + 1.0/i;//注意精度
		}
		else
			sum = sum - 1.0/i;

	}
	printf("This sum is %f\n",sum);
	return 0;
}
